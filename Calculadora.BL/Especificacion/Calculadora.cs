using System;
using System.Collections.Generic;
using System.Text;

namespace Calculadora.BL.Especificacion
{
    internal class Calculadora
    {
        /*
         * Hola mundo
         * asdfjasdkfj
         * asdfasdfas
         * dfa
         * sdf
         * asdf
         * asd
         * 
         * */
        public double Logaritmo(double x)
        {
          var  argX = x; 
            return argX;
        }

        // hola mundo
        // hola mundo
        public double Potencia(double x, double y)
        {
            var numero = x;
                return numero;
        }

        /// <summary>
        /// Este método se encarga de calcular la raíz cuadrada de un número.
        /// </summary>
        /// <param name="x">El número al que se le calculará la raíz cuadrada.</param>
        /// <returns>La raíz cuadrada del número.</returns>
        public double RaizCuadrada(double x)
       
        {
      
       var result = x;
      return result;
                
        }

        public double Suma(double x, double y)
        {
            var elResultado = x + y;
            return elResultado;
        }

        public double Tangente(double x)
        {
          var Result = x;
              return Result;
        }
    }
}
